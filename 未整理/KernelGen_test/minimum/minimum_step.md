1、kernelGen原始需求：

minimum：逐元素比较两个输入，返回对应位置的最小值。

```
基础版：
	操作符名字是 minimum，是一个 pointwise 算子，逐元素最小值运算。入参 2 个，分别是input，类型是 torch.Tensor，第一个输入张量；other，类型是 torch.Tensor，第二个输入张量或标量。输出的类型是 torch.Tensor，两个输入逐元素取最小值的结果。
	

```

2、优化任务书

3、算子分析

--》检索业界基准实现，根据实际情况纠正算子分析md的偏差错误

4、算子定义

--》仿照算子定义文档，差异化生成minimum的定义文档

--》再给出跑在不同硬件上的定义文档（Nvidia、天数、华为、海光、摩尔线程）

5、minimum算子生成与优化全流程文档
